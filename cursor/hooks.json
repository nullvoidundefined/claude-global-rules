{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "{{adapter}} sessionStart session-start hookspath-drift-check redaction-guard-check enforcement-guard-check parallel-session-check hook-integrity-check",
        "timeout": 30
      }
    ],
    "beforeShellExecution": [
      {
        "command": "{{adapter}} beforeShellExecution secret-scan no-em-dash fix-commit-requires-test conflict-markers commit-message-guard destructive-db-guard destructive-command-guard global-repo-push-guard git-workflow-guard push-eslint-gate push-ruff-gate push-rubocop-gate push-golangci-gate constant-change-guard audit-signal-check llm-rule-judge single-file-folder-gate build-cheatsheets",
        "timeout": 180
      }
    ],
    "afterShellExecution": [
      {
        "command": "{{adapter}} afterShellExecution redact-output",
        "timeout": 30
      }
    ],
    "beforeMCPExecution": [
      {
        "command": "{{adapter}} beforeMCPExecution mcp-action-guard destructive-db-guard",
        "timeout": 30
      }
    ],
    "preToolUse": [
      {
        "command": "{{adapter}} preToolUse secret-scan no-em-dash migration-defaults-guard structure-gate content-gate",
        "timeout": 30
      }
    ],
    "afterFileEdit": [
      {
        "command": "{{adapter}} afterFileEdit secret-scan no-em-dash migration-defaults-guard structure-gate content-gate new-file-header-reminder flat-directory-reminder spec-glossary-check clean-code-reminder observability-reminder dockerfile-reminder",
        "timeout": 60
      }
    ],
    "beforeReadFile": [
      {
        "command": "{{adapter}} beforeReadFile",
        "timeout": 10
      }
    ],
    "stop": [
      {
        "command": "{{adapter}} stop verification-gate ntfy-notify",
        "timeout": 660
      }
    ],
    "sessionEnd": [
      {
        "command": "{{adapter}} sessionEnd session-end",
        "timeout": 60
      }
    ]
  }
}
