# Rule misses log

Append-only. Two entry sources:
1. **Hook-written entries**: single-date `YYYY-MM-DD R-NNN <project> MISS <context>; gap: <what the rule would need>` lines, appended by `~/.claude/hooks/session-end.sh` per R-305.
2. **Retrospective consolidations**: date-range `YYYY-MM-DD..YYYY-MM-DD R-NNN (N occurrences) MISS <summary>; gap: <consolidated gap>` lines, written by the maintainer when collapsing many repeats of the same miss into a single counted entry.

2026-04-08..2026-05-27 R-101 (36 occurrences, retrospective consolidation) MISS Attempted to inspect a secret env var value by piping CLI output through grep/sed redaction. The sed pattern was fragile and failed to match, leaking a partial key prefix to chat. gap: R-101 needs an explicit "never pipe known-secret values through any display tool, including 'redacting' filters. Compare in-shell with `[ "$A" = "$B" ]` and print only the boolean result." Honor-system grep/sed redaction of secrets is a known-bad pattern because the redaction is fragile and the original value passes through the pipeline unredacted.
2026-05-27 R-101 MISS used a Railway CLI pipe through grep/sed thinking the sed would redact the value, but the sed pattern was fragile and leaked a partial key prefix to chat; gap: R-101 currently covers reading and committing secrets but does not codify the safe pattern for comparing or inspecting a known-sensitive env var. The rule needs an explicit "do not pipe known-secret values through any display tool, including 'redacting' filters, ever. Compare in-shell with `[ "$A" = "$B" ]` and print only the boolean result."
2026-05-27 R-101 MISS used `railway variables --service server | grep -i anthropic | sed 's/│.*│/│ <REDACTED> │/'` thinking the sed would redact the value, but the sed pattern depended on box-drawing characters and column alignment that didn't match cleanly, so ~35 characters of the rotated ANTHROPIC_API_KEY prefix were printed to chat after the user had explicitly said "do not echo, print, save, or send these values"; gap: R-101 currently covers reading and committing secrets but does not codify the safe pattern for *comparing or inspecting* a known-sensitive env var. The rule needs an explicit "do not pipe known-secret values through any display tool, including 'redacting' filters, ever. Compare in-shell with `[ "$A" = "$B" ]` and print only the boolean result." Honor-system grep/sed redaction of secrets is a known-bad pattern because the redaction is fragile and the original value passes through the pipeline unredacted up to the moment the filter runs.
